export async function getPin() { const pin = localStorage.getItem('pos_pin'); return pin ? pin : null; } export async function setPin(newPin) { localStorage.setItem('pos_pin', newPin); } export async function verifyPin(inputPin) { const pin = localStorage.getItem('pos_pin'); return !pin || pin === inputPin; } export async function changePin(oldPin, newPin) { const pin = localStorage.getItem('pos_pin'); if (pin && pin !== oldPin) throw new Error('Incorrect old PIN'); localStorage.setItem('pos_pin', newPin); }
