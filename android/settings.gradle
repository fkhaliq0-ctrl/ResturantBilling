include ':app'

File capacitorSettingsFile = new File(settingsDir, 'capacitor.settings.gradle')
if (capacitorSettingsFile.exists()) {
    apply from: capacitorSettingsFile
}

include(':capacitor-cordova-android-plugins')
project(':capacitor-cordova-android-plugins').projectDir = new File(rootProject.projectDir, 'capacitor-cordova-android-plugins')
